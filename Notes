1. /compile returns "success"; /compileAsync returns "success"; /compileStatus?operationId= returns "success"; /compileAsync returns "success"; /text/get returns "success"; /text/patch returns "success"; /git/startSession returns "success"; /git/commitPatch returns "success"; /git/pullPush returns "success"; /projects returns "success";  /statusSummary?projectId=demo returns "success"; /compile with xelatex returns "success"; /health returns "success"; /capabilities returns "success"; /metrics returns "success"; /text/get(path containment violation) returns "success"; /text/get(disallowed_extension) returns "success".

2. mark accomplished milestones with checkmark so I have a better overview. 

3. update applicable documents.

4. stage, commit and push.

5. add MCP tool registration and diagnostics enhancements as requested.

6. proceed with roadmap

7. remember to apply future tests post-implementation

cancel_operation: Introduce POST /cancel { operationId } and verify operation moves to cancelled and no artifact is created.
Debounce/coalescing + concurrency limits: Stress compileAsync for same project and verify dedupe.
tex_outline, analyze_structure, preflight, lint/format: Add endpoints; test AST structure and diffs/diagnostics.
Async events (WS/SSE): Subscribe and assert operation.updated/final sequence.
Containerized sandbox: Enforce caps and verify kills on overuse.

